{
  "name": "any-db-common",
  "version": "3.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "prepare": "tsc --build",
    "clean": "rm -rf dist",
    "docs": "typedoc --out docs",
    "test": "node test.js"
  },
  "devDependencies": {
    "@types/node": "^10.5.3",
    "typescript": "^3.0.0"
  }
}
